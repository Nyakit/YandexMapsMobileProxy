    import XCTest
    @testable import YandexMapsMobileProxy

    final class YandexMapsMobileProxyTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(YandexMapsMobileProxy().text, "Hello, World!")
        }
    }
